name: Daily Robotics Paper Email

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  send-paper:
    runs-on: ubuntu-latest
    timeout-minutes: 6
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Send daily paper email
        env:
          QQ_SMTP_USER: ${{ secrets.QQ_SMTP_USER }}
          QQ_SMTP_AUTH_CODE: ${{ secrets.QQ_SMTP_AUTH_CODE }}
          MAIL_TO: ${{ secrets.MAIL_TO }}
          ZOTERO_API_KEY: ${{ secrets.ZOTERO_API_KEY }}
          ZOTERO_USER_ID: ${{ secrets.ZOTERO_USER_ID }}
          ZOTERO_COLLECTION_NAME: ${{ secrets.ZOTERO_COLLECTION_NAME }}
          OBSIDIAN_OUTBOX_ROOT: obsidian-outbox
          OBSIDIAN_PROJECT_ROOT: ResearchVault/legged-robot-motion-control
        run: python scripts/send_daily_robotics_paper.py

      - name: Commit sent paper history and Obsidian outbox
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/sent_papers.json obsidian-outbox paper_preferences.json
          git diff --cached --quiet || git commit -m "Record sent paper and Obsidian outbox"
          git push
